The updateClock() function gets the current time using the Date() object. 
It then formats the time into hours, minutes, and seconds using string manipulation and 
updates the content of the <div> element with the formatted time.

We call the updateClock() function once to initialize the clock and 
then use the setInterval() method to update the clock every second by calling the updateClock() function.
